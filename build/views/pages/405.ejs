<!DOCTYPE html>
<html lang="en">
  <head> </head>

  <body class="container">
    <header></header>

    <main>
      <p>This is 404fads page</p>
      <%- include('../partials/include'); %>
    </main>

    <% if(true) { %>
    <p>if</p>
    <% } else{ %>
    <p>else</p>
    <% } %>
    <footer></footer>
  </body>
</html>
